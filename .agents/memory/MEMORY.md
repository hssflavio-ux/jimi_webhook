# Memory Index

> Auto-load at session start. Max 200 lines.

## User
- [user] Flavian, Windows 11, PowerShell 7+, VS Code/Cursor → user-preferences.md
- [user] Português (PT-BR), respostas concisas, prefere ação a explicação → user-preferences.md
- [user] PHP lint via `C:\Users\flavi\php\php.exe -l` → user-preferences.md

## Project
- [project] jimi_webhook v4.3.0 — YUV Parity, PHP 8.3 puro + MySQL 8.0 → project-conventions.md
- [project] Design system Coinbase: azul #0052ff, sidebar #0a0b0d, CTAs pill 100px, JetBrains Mono números/IMEI → project-conventions.md
- [project] Autenticação token cookie `jimi_token` → MySQL `sessions`, sem `session_start()` — $_SESSION é POR REQUEST (nunca persistir nada nele entre requests) → project-conventions.md
- [project] PROJETO_YUV.md é o contrato-mestre; STATUS.md é o diário vivo (estado atual: §12) → project-conventions.md
- [project] Fases 0-M + iteração v4.1.1 concluídas — comandos ponta-a-ponta OK (síncrono + callback offline real), BRT em todo o dashboard, Playwright 37/37 → project-conventions.md
- [project] Servidor homolog: 189.22.240.43 (host `iothub`) Apache/FPM no HOST + 16 containers IoTHub; containers só alcançam o host via IP LAN 10.1.0.43 (nunca localhost) → project-conventions.md
- [project] Deploy homolog (12/07/2026): servidor puxa de hssflavio-ux/jimi_webhook (deploy key dedicada no root; repo Flaviohses é legado); SSH por chave como administrador + sudo com senha; usuário E2E e2e@teste.local existe no homolog; working copy do servidor é www-data (git pull só com sudo) → project-conventions.md
- [project] Estado homolog 22/07/2026: v4.3.0 implantado em `5f6b8ed` (banco 4.3.0). Relatório de deslocamento em 2 modalidades (viagens | fechamento diário) + mapa de rota (/relatorios/deslocamento/rota) + teto global 31 dias (clamp_report_range) + índice trips(customer_id, started_at). QUANDO deploy.sh muda a si mesmo (bloco de migration novo): rodar `sudo ./scripts/deploy.sh && sudo ./scripts/deploy.sh --force` (git pull no meio do script → bloco novo só vale na 2ª passada; sudo cacheia a senha) → tech-decisions.md
- [project] Auto-vídeo de evento (v4.2.1, 13/07): ocorrência nova JT/T dispara **37384/0x9208 (Alarm Attachment Upload)** pós-commit com alarmLabel do push + alarmNumber=bin2hex(IMEI[-14:]+label[14:]) + attachment server (ingest_ip:21188, overrides ATTACH_UPLOAD_IP/PORT). **34818/0x8802 é CONSULTA (não upload)** — JC371 real respondeu mediaItemsNum:0 para evento DMS. Arquivos chegam {imei}_{label}_{xy}.mp4/.jpg via /pushfileupload; vínculo mídia→ocorrência pelo label (alarms.alarm_label→occurrence_events), vídeo > imagem, fallback ±3min. Kill-switch AUTO_VIDEO_REQUEST=0 → tech-decisions.md
- [reference] Câmeras: device_models.camera_count = MÁXIMO do modelo (JC182=1, JC181/JC400D/JC400AD=2, JC371≤3, JC450≤5); devices.camera_count = instalado (cadastro). Telas de vídeo usam COALESCE(NULLIF(d.camera_count,0), dm.camera_count, 1) — nunca ler só o modelo (2e8472f) → tech-decisions.md
- [project] Estado 02/08/2026 (v4.8.3 **PUBLICADA no homolog**, `671887c`, `/ping` 4.8.3; **produção intocada**): PDF dos relatórios com **larguras de coluna por peso** (`stream_export(..., $colWeights)`) + **quebra de linha** por métrica AFM real do Helvetica — antes toda coluna tinha a mesma largura e o endereço saía cortado. `report_period_label()` é o ponto único do "Período: DD/MM/AAAA HH:MM:SS a …" (sem `(BRT)`, hora sempre explícita) nos 9 relatórios que exportam PDF. Verificado pós-deploy em 93 páginas / 23.100 textos: 0 vazando a coluna, 0 truncados. **Ressalva**: os endereços do homolog têm ≤72 chars e cabem — a QUEBRA em si não foi exercitada por dado real, só por teste sintético → tech-decisions.md
- [reference] **A Alarm Reference da doc oficial ESTÁ no HTML servido** — a nota da migração v4.8.1 dizia que era hidratada por JS e exigia DOM renderizado, e isso é FALSO: `Invoke-WebRequest https://docs.jimicloud.com/integration/integration.html` traz 803 KB com as tabelas prontas; recortar de `id="_1-jimi-device-alarms-msgclass-0"` e parsear os `<table>`. A conferência que isso permitiu achou 7 subtipos DMS (265-X) DESLOCADOS em `alarm_types` → tech-decisions.md
- [project] `alarms.alarm_name` é DESNORMALIZADO (pushalarm resolve na chegada e grava a string): corrigir `alarm_types` NÃO conserta o histórico — precisa de `UPDATE alarms JOIN alarm_types`, excluindo o 256 (nome vem do bitmask, `alarm_subtype` guarda o VALOR) e 264/265 sem subtipo → tech-decisions.md
- [feedback] deploy.sh: git pull roda no MEIO do script — mudança no próprio deploy.sh só vale na próxima execução (rodar deploy 2× quando o script muda) → feedback-history.md
- [project] IoTHub: comandos via :10088 (segura resposta até 30s aguardando device — timeout do app é 35s); vídeos servidos pelo dvr-upload :23010 (Apache NÃO acessa /iothub/dvr-upload) → project-conventions.md
- [project] Devices reais no homolog: 860112070347838 (JC181) e 869058070151343 (JC182), ambos JTT serverFlagId=0 → project-conventions.md
- [project] Dev Windows tem MySQL 8.0.37 portátil em C:\Users\flavi\mysql (subir com scripts/dev-windows.ps1); usuário E2E: e2e@teste.local → project-conventions.md
- [project] Estado 30/07/2026 (noite): código em **v4.7.1** — o `docs/PLANO_IMPLEMENTACAO_v4.4-v4.7.md` está **COMPLETO (Fases 1–5)**. **Homolog PUBLICADO em `a1a879b` / banco 4.7.0**, com os 7 workers no cron e backfill do state_builder feito (o STATUS.md que dizia `92725cb`/4.4.1 estava desatualizado — CONFERIR o servidor antes de confiar no arquivo). v4.7.1 é só código (SEM migração): token no nome dos arquivos de `storage/`, `REPORT_RETENTION_DAYS`, "Expirado" em `/exportar` e a `/wiki` atualizada da v4.3.0 para a v4.7.1. **O que falta da iniciativa não é código**: Bloco 2 do `docs/PLANO_VALIDACAO_AGENDAMENTOS.md` (envio agendado contra provedor real — exige caixa de entrada do usuário) → project-conventions.md
- [project] **v4.7.1 — `storage/` é servido como ESTÁTICO e nunca passou por `require_login()`** (o `.htaccess` da raiz só reescreve o que não é arquivo, `!-f`). Comprovado no homolog: `curl` sem cookie em `storage/reports/x.txt` → **200 + conteúdo**. Nome de relatório/vídeo agora leva 32 hex de `random_bytes(16)` (era `report_<job_id>_<timestamp>`, enumerável = vazamento entre tenants). NÃO virou rota autenticada de propósito: o link do relatório grande no e-mail precisa abrir sem sessão. Alvo melhor e ainda pendente: URL assinada com validade (token não protege link vazado). `storage/.htaccess` versionado (`.gitignore` = `storage/*` + `!storage/.htaccess`; negação dentro de diretório excluído o git ignora) → tech-decisions.md
- [project] **v4.7.1 — retenção**: `REPORT_RETENTION_DAYS` (padrão 30, `0` desliga) no `log_cleanup.php` purga `storage/reports` + `report_schedule_runs`. Ler com `getenv() === false ? '' : trim()`, **nunca com `?:`** — `'0'` é falsy e o operador devolveria 30 justo para quem quis desligar. Conexão PDO em try/catch (não `Database::getInstance()`, que dá exit): banco fora só pula a purga do histórico. `storage/media` fica INTOCADO (vídeo de ocorrência é evidência) → tech-decisions.md
- [reference] **`bash -n` falha nos `.sh` no Windows e isso é FALSO POSITIVO**: `core.autocrlf=true` deixa o working tree em CRLF, mas o índice (o que o servidor recebe) é LF. Para lintar de verdade: `git show :scripts/deploy.sh | bash -n`. Idem porta **8124**, recusada pelo Windows (faixa excluída) — usar 8123 para servidor de teste local → tech-decisions.md
- [project] **v4.7.0 — fuso é o risco da fase**: `report_schedules.send_hour` é BRT, `next_run_at` é UTC; converter só por `DateTimeZone` (`includes/schedule.php`), NUNCA somando 3 h — erra para datas com DST (até 2019) e voltaria a errar se a política mudar. Reentrância: o dispatcher move `next_run_at` ANTES de enfileirar, com `WHERE next_run_at = <valor lido>`, então cron sobreposto não envia 2×. Período é sempre o FECHADO anterior (ontem/semana passada/mês passado) → tech-decisions.md
- [project] **v4.7.0 — entrega vs. artefato**: quando o relatório gera mas o e-mail falha, o job fica `concluido` (o arquivo existe e é baixável em `/exportar`) e a EXECUÇÃO fica `falhou` em `report_schedule_runs`. É o histórico que alimenta a regra das 3 falhas consecutivas (sucesso ZERA o contador). Acima de `MAIL_MAX_ATTACH_MB` o anexo vira link, usando `APP_URL` como base → tech-decisions.md
- [feedback] **Edição automatizada em lote exige auditoria arquivo por arquivo.** Um script injetou a barra de modelos em 9 handlers com regex `/^require_login\(\);\n/m`; `rel_geocercas.php` é o ÚNICO arquivo do repo com **CRLF**, então o `require_once` não entrou e a tela passou a dar "Erro interno" (`Call to undefined function`). **`php -l` NÃO pega isso** — a função existe, só não está carregada. Quem pegou foi a suíte Playwright completa. Depois de qualquer sed/regex em lote: auditar a presença de TODAS as peças em TODOS os arquivos e fazer smoke das rotas → feedback-history.md
- [project] **v4.6.0 — a invariante da segmentação**: `device_state_segments` é contígua e sem sobreposição (`ended_at` de um = `started_at` do seguinte) ⇒ **soma das durações de um dia = 86.400 s exatos**. É o único teste que pega furo de segmentação; quem mexer no `state_builder.php` preserva isso. Mudança de estado → fronteira no ponto NOVO; buraco (≥30 min) → fronteira no ponto ANTERIOR + segmento `offline` cobrindo o vão. Último segmento fica ABERTO (`ended_at NULL`) e é reescrito por ON DUPLICATE KEY na rodada seguinte. Segmento de duração zero não é gravado (colidiria com o `offline` na chave `(imei, started_at)`) → tech-decisions.md
- [project] **Estado corrente ≠ segmento aberto**: `/relatorios/status-frota` resolve o estado na LEITURA (`resolve_current_state()`, includes/fleet_state.php) — quem não reporta há 30 min vira `offline` qualquer que fosse o estado anterior. Um veículo calado desde as 3h tem segmento aberto em `movimento` e dizer "em movimento" às 10h seria mentira; entre 2 rodadas do cron a verdade muda sem dado novo no banco → tech-decisions.md
- [project] **Limiares de estado moram em `includes/fleet_state.php`** (STOP_SPEED_KMH=3, STOP_IDLE_SECONDS=300, OFFLINE_GAP_SECONDS=1800, DEFAULT_SPEED_LIMIT_KMH=80, MIN_SPEEDING_POINTS=2) e são consumidos por `trip_builder.php` E `state_builder.php`. Nunca redeclarar local: "parado" tem de significar o mesmo nos dois, senão as telas se contradizem (risco R6 do plano) → tech-decisions.md
- [feedback] **Spec escrito não é spec verificado**: os 9 casos de `tests/geocercas.spec.js` (v4.5.0) nunca tinham sido executados com credenciais e, ao rodarem na Fase 3, 2 falharam de imediato — um deles revelando bug real (`/geocercas` re-renderizava o formulário vazio após salvar; F5 duplicava a cerca). Rodar a suíte COM `TEST_EMAIL`/`TEST_PASSWORD` antes de dar fase por concluída → feedback-history.md
- [project] **`deploy.sh` NÃO instala cron** — worker novo exige `bash scripts/crontab-setup.sh --install` à parte, senão a falha é silenciosa (tela funciona, relatório vazio para sempre). O `deploy.sh` JÁ atualiza `SYSTEM_VERSION` do `.env` sozinho (sed a partir do `.env.example`) e o gate de migração é semântico (`sort -V`, roda só o que falta) desde `4e60322` → tech-decisions.md
- [project] **Tela nova nasce sem permissão**: `can()` (includes/auth.php) nega tela ausente da matriz JSON do grupo, então usuário em grupo restrito toma 403 até o admin marcar a linha em `/grupos-permissao`. Admin (`{"*":[...]}`) e usuário sem grupo passam. Vale para toda tela nova desde a v4.4.x → tech-decisions.md
- [reference] Distância: usar **`haversine_km()`** (includes/functions.php, promovida do trip_builder na v4.5.0). **NUNCA `calculate_distance()`** para teste geográfico novo — ela retorna 0 quando qualquer latitude é 0, o que torna todo ponto no Equador "dentro de tudo"; fica só por causa de chamador legado (pushgps.php) → tech-decisions.md

## Feedback
- [feedback] Usuário diz "Continue" para avançar fases (não perguntar se quer continuar) → feedback-history.md
- [feedback] Prefere implementação completa por fase com verificação lint ao final → feedback-history.md
- [feedback] Valoriza STATUS.md atualizado como artefato de handoff entre sessões → feedback-history.md
- [feedback] Wiki (/wiki) é para USUÁRIO FINAL: sem termos técnicos (proNo/AJAX/polling), sem caminhos de URL (referir telas pelo menu lateral), mapas com imagem real (assets/img/wiki_map_*.png), sem seções de webhook/motor/segurança → feedback-history.md

## Reference
- [reference] LOGS: LOG_LEVEL no .env (lazy — .env só existe após 1º Database::getInstance(); DEBUG liga RAW_WEBHOOK_DATA); purga/rotação = cron diário scripts/log_cleanup.php (NÃO usa classe Database — ela dá exit com banco fora); handler global de exceção/fatal do dashboard em auth.php; `php -r` NÃO dispara set_exception_handler (testar com arquivo) → tech-decisions.md
- [reference] VÍDEO HISTÓRICO (cartão): LISTAR = 37381/0x9205 (janela GMT-0 compacta yyMMddHHmmss que NÃO cruza o dia → fatiar por dia UTC; resposta assíncrona → /pushresourcelist → resource_lists, push §1.11 pode vir SEM data_list). Timeline do /video/playback une resource_lists+media_files (merge ±120s). resourceType do 0x1205: 0=áudio+vídeo (NÃO é imagem). ATENÇÃO: [Extrair] usa 34818/0x8802 que é CONSULTA — extração real de gravação do cartão = 37382/0x9206 e exige FTP do cliente (doc §2.7; pendência de infra) → tech-decisions.md
- [reference] VÍDEO AO VIVO (37121/0x9101): o comando manda o DEVICE PUBLICAR o RTP no media server — videoIP/videoTCPPort devem ser o endereço que o DEVICE alcança (IP público do servidor + porta ingest 10002), NUNCA window.location.hostname nem porta 0. dataType:0=vídeo. Usar helper video_stream_config() (flv_base saída FLV :8881; ingest_ip/port; playback_port 10003; overrides VIDEO_INGEST_IP/PORT no .env). FLV URL: STREAM_URL/{canal}/{imei}.flv. Player precisa RETRY (device leva 5–30s para publicar) — 1 tentativa única falha. Validado com JC182 real (2 MB FLV capturado). → tech-decisions.md
- [reference] TIMEZONE: armazenar UTC, exibir BRT SEMPRE via fmt_brt(); filtros de dia via brt_day_range_to_utc(); defaults brt_today(); GROUP BY hora/dia via CONVERT_TZ(col,'+00:00','-03:00'); colunas DATE puras NÃO convertem → tech-decisions.md
- [reference] Gateway auto-cria devices órfãos (customer_id NULL) na 1ª telemetria — cadastro em /ativos/novo ADOTA a linha (nunca recusar por COUNT global de IMEI) → tech-decisions.md
- [reference] Resposta síncrona de comando vem no HTTP response do sendInstruct (data._content) → status 'executed' imediato; offline (_code 600) fica 'sent' até callback em /pushinstructresponse (objeto único §2.4, allowSingleObjectPayload) → tech-decisions.md
- [reference] Migration v4.1.0: jobs.format + fix seed occurrence_config_params (nomes reais de alarm_types) → tech-decisions.md
- [reference] Motor de ocorrências: pushalarm → occurrence_engine → ocorrências (dedup 10min); matching exige nome EXATO de alarm_types → tech-decisions.md
- [reference] CSRF: token derivado por HMAC-SHA256(cookie jimi_token, WEBHOOK_TOKEN) — NÃO usar $_SESSION para persistir token (não há session_start) → tech-decisions.md
- [reference] Exportação: includes/export_helper.php — XLSX via ZipArchive + PDF 1.4 puro-PHP, SEM Composer (decisão: projeto é "no package manager") → tech-decisions.md
- [reference] pushalarm: capturar lastInsertId() ANTES de callProcedure() — CALL reseta o valor para 0 → tech-decisions.md
- [reference] devices.last_position_at NÃO existe — última posição vem de device_statistics.last_gps_time (JOIN) → tech-decisions.md
- [reference] Workers cron: worker.php (jobs csv/xlsx/pdf), trip_builder.php (haversine), metrics_rollup.php → tech-decisions.md
- [reference] Paginação (v4.3.0): usar `report_pagination($page,$totalPages,$totalRows,$unit)` — o laço `min($totalPages,10)` que estava copiado em 7 telas travava o contador na página 10 (não mostrava a página atual a partir da 11ª). Faixa horária opcional = `report_time_window()` com 2 modos escolhidos na tela (`time_mode`): CONTÍNUA (data/hora inicial → data/hora final) ou EM CADA DIA do período (`TIME(CONVERT_TZ(...))`; hora inicial > final = turno que cruza a meia-noite, usa OR) → tech-decisions.md
- [reference] Relatórios — UI comum (v4.3.0): TODO relatório com data abre em ordem CRESCENTE (mais antigo no topo) e tem seta clicável na coluna de data/hora — usar `report_sort_params($whitelist, $default, 'ASC')` + `report_sort_link()` (includes/functions.php; a whitelist é obrigatória, a coluna vai interpolada no SQL) e `report_back_button('/relatorios/xxx')` + `report_has_query()` para o `← Voltar` à tela limpa. Export deve usar a MESMA variável de ordenação da grade. Cuidado: query com `ORDER BY … DESC LIMIT N` (hub /relatorios) NÃO pode virar ASC no SQL — inverter em PHP, senão a amostra troca pelos N mais antigos → tech-decisions.md
- [reference] Relatórios (v4.3.0): teto GLOBAL de 31 dias via clamp_report_range() — todo relatório novo com filtro de data DEVE aplicá-lo; deslocamento tem 2 modalidades (viagens | fechamento diário GROUP BY dia BRT) + mapa de rota (/relatorios/deslocamento/rota, trip_id ou imei+dia); trips indexada por (customer_id, started_at) — queries por período dependem desse composto → tech-decisions.md
- [reference] Deploy: scripts/deploy.sh aplica migrations até v4.1.0; deploy-v4.sh (--check/--migrate/--deploy/--verify) → tech-decisions.md
- [reference] Router: `$renamedRoutes` para rotas com hífen (config-ocorrencias, grupos-permissao) — rota nova com hífen vai ali, não em $simpleRoutes → tech-decisions.md
- [reference] Testes: npx playwright test (tests/, 15 specs; autenticados pulam sem TEST_EMAIL/TEST_PASSWORD); helpers em tests/helpers rodam pelo scripts/run-tests.ps1 (5 .test.php + 1 .test.js); bash scripts/test_e2e.sh (replay webhooks) → tech-decisions.md
- [project] 🔴 **A suíte Playwright NÃO rodava** (achado na v4.9.32): o `testMatch` padrão coleta `*.test.js`, e `tests/helpers/player_snapshot.test.js` é script Node autônomo que termina em `process.exit()`. A importação matava o processo do Playwright ANTES do primeiro spec, e a saída era "Running 137 tests" seguida de nada — **com exit 0**, que CI lê como verde. Corrigido com `testMatch: '**/*.spec.js'` no playwright.config.js. Passar um caminho (`npx playwright test tests/x.spec.js`) sempre funcionou, e é por isso que sobreviveu → tech-decisions.md

- [project] **v4.9.32 — firmware**: `UPDATE,P1#` estava travado em JC371 porque `universal` no `command_catalog.php` é DERIVADO da wiki ("5+ das 6 páginas") e só a página do JC371 documenta o comando. Ele vale para a linha JC inteira; **o que muda é só a URL do pacote**. Escolher `UPDATE` desabilitava 9 de 10 equipamentos. Agora universal, com exceção manual documentada e travada em teste. Em troca: **um modelo por vez** no envio em lote (a URL errada não devolve erro — o device baixa e aplica), URL cadastrada **por modelo** em `/firmwares` (tabela `firmware_releases`), e a resposta do `VERSION#` gravada em `devices.firmware_version` nos dois caminhos (`sendcommand.php` síncrono + `pushinstructresponse.php` callback). O botão FOTA de `/equipamentos` mandava **proNo 33027** com payload inventado e nunca atualizou nada → tech-decisions.md

- [project] **Acesso a produção por CHAVE desde 19/08/2026**: a pública do Windows (`flavi@dev-windows-jimi`, `SHA256:lHHRen2…`) foi instalada em `administrador@186.248.143.197`, ao lado da do Mac (`claude-code`). Antes disso produção recusava a Windows e só aceitava senha. `PasswordAuthentication` segue ligado (escape hatch); backup em `~/.ssh/authorized_keys.bak-20260819`. 🔴 Entrar por chave **não** dispensa a senha do `sudo` — para deploy não-interativo: `printf '%s
' <senha> | sudo -S -p '' ./scripts/deploy.sh`. Quando a chave não vale (ex.: máquina nova), o `plink` do PuTTY (`C:\Program Files\PuTTY\plink -pw`) resolve sem prompt, com `-hostkey` tirado de `ssh-keygen -l -F <ip>` — e a sessão do plink abre no HOME, não no projeto → `sudo: ./scripts/deploy.sh: command not found` sem o `cd` → tech-decisions.md

- [project] **v4.9.33 — FILELIST**: a câmera SEMPRE mandou a lista; o Apache é que descartava. Corpo `chunked` acima de **16 KB** sumia em silêncio entre Apache e PHP-FPM (`mod_proxy_fcgi`) — HTTP 200, nada no error.log, `php://input` vazio. Medido por busca binária (16.293 B chegam, 16.699 B viram 0) e isolado por experimento (o mesmo corpo chega inteiro ao `php -S` sem Apache). Corrigido com `SetEnvIf Request_URI "^/filelist/" proxy-sendcl=1` (`docs/apache/filelist-chunked.conf`) — **infra fora do git**, precisa de `a2enconf`. O retorno é JSON: `{"imei":…,"fileNameList":"a.ts,b.ts,…"}`, 3.021 nomes na 400AD_3. 🔴 **`<Location>`/`<LocationMatch>` NÃO valem para rota nenhuma deste projeto**: o `.htaccess` reescreve tudo para `handlers/router.php` e o location walk roda contra a URI reescrita — a diretiva não é aplicada e falha em SILÊNCIO (config no ar, configtest OK, zero efeito). Usar `<Directory>` ou `SetEnvIf` → tech-decisions.md
- [project] **v4.9.34 — a lista do FILELIST passou a ser LIDA** (`includes/filelist.php` → `resource_lists`, a mesma tabela do JT/T). 🔴 **O carimbo do nome é hora LOCAL da câmera (UTC−3), NÃO GMT 0** — a única parte que falharia em silêncio (datas plausíveis 3 h fora do lugar). Medido contra os anexos de alarme das TRÊS câmeras JIMI, que trazem o mesmo carimbo e cujo `alarm_time` é conhecido: `EVENT_…_2026_08_20_08_47_42_I_15.ts` ↔ `11:47:42` UTC, **+3 h em 29/29**. "O device transmite GMT 0" vale para o CORPO do webhook, não para o nome do arquivo. Bloco do cartão = **1 minuto** (doc A010 + 1.442 de 1.461 intervalos com 60 s cravados); `end_time = min(próximo início, +60 s)`. Dois defeitos saíram junto, ambos "comando aceito, nada acontece": a tela mandava só `FILELIST,<url>` (que só CONFIGURA o endereço) e nunca o `FILELIST` **nu**, que dispara — provado nos dados (7 comandos com URL, zero capturas; o nu de 15:00:19 gerou a captura de 15:00:19); e o `[Extrair]` mandava **37382** (JT/T) para câmera JIMI, cujo comando é `HVIDEO,<carimbo>,<câmera>`. Verificado com a captura real: 3.021 nomes → 3.021 linhas, reenvio idempotente → tech-decisions.md
- [feedback] **Quando um nome de arquivo carrega hora, o fuso dele é uma MEDIÇÃO, não uma convenção.** Bastava um `SELECT file_name, event_time` para descobrir o offset — o dado estava no banco de produção desde sempre. A convenção do projeto ("device transmite GMT 0") teria levado ao erro, e ele não apareceria em teste nenhum: 3 h de desvio numa lista de 4 dias continua parecendo certo → feedback-history.md

- [feedback] **Teste de infraestrutura tem de reproduzir o TAMANHO, não só o protocolo.** O FILELIST ficou 5 dias parado porque um teste anterior "descartou a infraestrutura" com POSTs chunked de 17 e 28 bytes — abaixo do limite de 16 K, passavam. E **conferir que o comando existe antes de ler a saída dele como evidência**: `strings ... | grep proxy-sendcl` deu vazio e quase virou "o módulo não suporta"; o `strings` não existe no servidor. Refeito com `grep -a` + controle, o resultado se inverteu — e era a correção certa → feedback-history.md

## Pendências (exigem produção/dispositivo — ver STATUS.md §11.4, §12.7 e §12.13)
- Deploy do fix 37384 no homolog (sudo) + validar com eventos DMS reais da JC371 865478070003241 (vídeo deve chegar via /pushfileupload e aparecer na ocorrência)
- [Extrair] do playback: JT/T usa 37382 desde a v4.9.1 (precisa do servidor FTP configurado); **JIMI usa `HVIDEO` desde a v4.9.34 e ainda não foi testado em câmera real** — verificado só até o despacho (spec de navegador). ⚠️ O "34818" desta linha estava desatualizado.
- OTA firmware: testar `UPDATE,<url>#` (proNo **128**) com dispositivo real — falta a URL do pacote, que só o fornecedor tem. ⚠️ O "proNo 33027" desta linha estava ERRADO: 33027 é "Definir parâmetro" do JT/T, não OTA (v4.9.32)
- Specs multi-tenant: exigem credenciais de segundo cliente (TEST_EMAIL_B/TEST_PASSWORD_B)
- deploy.sh: mysqldump falha silenciosamente no homolog (backup de banco não sai) — investigar privilégios
- Limpeza opcional no homolog: device teste 868120246598152 + usuário e2e@teste.local + ocorrências/mídias de teste
