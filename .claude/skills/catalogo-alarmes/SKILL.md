---
name: catalogo-alarmes
description: "Catálogo de alarmes (alarm_types): nome resolvido uma vez na chegada do webhook, renomear alarme ou categoria quebra o motor de ocorrências e as regras de notificação, código JT/T base contra composto, catálogo JIMI completo, colisão do 262, nome repetido entre códigos, is_driving (Alertas Dirigibilidade) e anexo em media_files. Use ao cadastrar ou renomear código de alarme, escrever migração que mexe em alarm_types, ou mexer em pushalarm.php, occurrence_engine.php, notification_engine.php, rel_alarmes.php ou rel_dirigibilidade.php."
---

> Blocos movidos **sem alteração** do `CLAUDE.md` em 23/09/2026 (`/doctor`: carregamento sob demanda — o `CLAUDE.md` é lido em toda sessão, esta skill só quando o assunto aparece). Na raiz do `CLAUDE.md` ficou um stub com a regra crítica. Referências como "o bullet acima/abaixo" apontam para outro bloco do `CLAUDE.md` ou desta skill ou da skill `mapa-risco`.

- **Renomear alarme em `alarm_types` quebra o motor de ocorrências se você parar aí.** `occurrence_config_params.alarm_type` guarda o **nome** do alarme, não o código, e `get_occurrence_param()` (`includes/occurrence_engine.php`) resolve o parâmetro por `JOIN alarm_types ON at.alarm_name_pt = ocp.alarm_type`. Nome renomeado = JOIN não casa = **nenhuma ocorrência é gerada**, em silêncio: o alarme é gravado, aparece nos relatórios, e a ocorrência simplesmente não nasce — sem erro no log nem na tela. Foi o que a v4.8.3 fez com 21 dos 41 parâmetros do homolog, e só apareceu quando `webhook_occurrence.spec.js` saiu do estado "pulado". **Toda migração que mexer em `alarm_name_pt` tem de remapear `occurrence_config_params` junto** (ver `migration_v4.8.6.sql`) e conferir com `SELECT` de órfãos.
  - **`alarm_types.category` é a MESMA armadilha, numa segunda tabela.** `notification_rules.alarm_type` aceita nome **ou categoria**, e `notification_engine.php` casa por `at.category = nr.alarm_type`. No homolog as **6** regras casam por categoria e **nenhuma** por nome — então renomear categoria sem remapear a regra desliga a notificação em silêncio, exatamente como o caso acima. A v4.9.5 unificou as categorias (estavam em inglês nas linhas JIMI e em português nas JT/T) e precisou remapear `notification_rules` junto, com `UPDATE IGNORE` por causa da `UNIQUE KEY (customer_key, alarm_type)`. `DMS` e `ADAS` ficam intocados: são siglas, e `rel_alarmes.php` filtra por `category IN ('DMS','ADAS')`. A tradução para a tela é na **exibição**, por `alarm_category_label()` — nunca gravando o rótulo traduzido na coluna.
  - 🔴 **O código JT/T que chega aos motores é a BASE, não o do catálogo.** `pushalarm.php` passa `alarm_type = '264'`; `alarm_types.alarm_code` guarda `'264-4'` (o subtipo vive em coluna separada, `alarms.alarm_subtype`). Enquanto os dois motores compararam só a base, **nenhuma regra por CATEGORIA ou por CÓDIGO disparava para DMS/ADAS de JT/T** — o núcleo do produto — enquanto JIMI (códigos simples) e JT/T sem subtipo (`1027`) funcionavam. Sintoma: câmera JIMI notifica, câmera JT/T não, sem erro em log nem tela. Corrigido na v4.9.6 passando `alarm_subtype` adiante e casando também pelo composto. **Regra/parâmetro gravado por NOME sempre escapou disso** (é o ramo `= :aname`), e é por isso que o motor de ocorrências nunca exibiu o defeito: os parâmetros são por nome, as regras de notificação são por categoria.
  - ⚠️ **Conferência de "sobrou algo em inglês?" precisa de `BINARY`.** A collation é `utf8mb4_unicode_ci`: sem ele, `WHERE category IN ('Video','Sensor')` casa os valores **já corrigidos** (`video`, `sensor`) e a migração acusa erro onde não há.

- **O nome do alarme é resolvido UMA vez, na chegada do webhook.** `pushalarm.php` consulta `alarm_types` e grava o resultado em `alarms.alarm_name`; código fora do catálogo naquele instante vira `Código NNNN (JTT)` e fica gravado assim **para sempre**, mesmo depois de o código entrar em `alarm_types`. Por isso `rel_alarmes.php` re-resolve na leitura — mas só o rótulo genérico: o nome gravado vence sempre que for um nome de verdade, senão o `Fim de Alarme: ` (evento de FIM) e o bitmask decodificado do JT/T 256 seriam apagados pelo nome do catálogo. Códigos novos entram por migração (`migration_v4.9.0.sql`, `migration_v4.9.10.sql`). A regra é **nunca batizar por palpite** — não "só o que a doc publica": `1047` não consta da doc e foi catalogado como `Capotamento` na v4.9.10 com informação do fornecedor, corroborada pelo bit 28 do bitmask JT/T (`Pré-aviso de Capotamento`) e pela ordem das unidades ADAS na faixa 1042–1046.
  - 🔴 **O catálogo JIMI está COMPLETO desde a v4.17.0: 197 códigos, o total da Alarm Reference oficial.** Nenhum código publicado pela fabricante cai mais como `Código NNNN (JIMI)`. Ao cadastrar um código novo, o recorte da doc tem de ser **por SEÇÃO** (`id="_1-jimi-device-alarms-msgclass-0"`, com 9 subseções que dão a `category` de graça) — varrer o documento inteiro engole as tabelas de **bits de status** (`0: ACC OFF 1: ACC ON`), que também têm duas células com número na primeira, e "descobre" que o alarme 2 significa "0: North latitude".
  - 🔴 **O espaço de códigos JIMI (até 262) CRUZA o do JT/T (que começa em 256), e há uma colisão real:** `262` é `Fim de Movimento` em JIMI e `Comportamento de Condução Irregular` em JT/T. **Não é duplicidade e não se apaga**: a chave é `(alarm_code, protocol)` e `alarm_label_sql()` desempata por `msg_class`. Travado em `tests/helpers/diagnostico_guard.test.php`, que também exige que **só o 262** exista nos dois protocolos — número novo caindo nessa faixa tem de ser decidido, não descoberto depois.
  - ⚠️ **Nome REPETIDO entre códigos é deliberado** — a fabricante publica o mesmo evento em mais de um código (`Capotamento` = 45+106+183, `Excesso de Velocidade` = 6+135, `Porta Aberta` = 28+80…) e o filtro dos relatórios casa por `alarm_name_pt`: nomes diferentes partiriam o evento em dois chips e o operador perderia metade dos registros. 🔴 **A consequência é que o código novo HERDA o parâmetro de ocorrência do irmão** (`occurrence_config_params` casa por nome) — foi o que aconteceu com 90/106/135/183 na v4.17.0, e é o comportamento certo, mas confira a lista depois de cadastrar: nome novo que colida por acaso com um parâmetro existente passa a abrir ocorrência sem ninguém ter decidido isso.
  - 🔴 **A Alarm Reference OFICIAL é a régua, e ela desmentiu dois rótulos de uma vez (v4.16.1).** `254` era `Status de Ignição Alterado` — a doc publica `254 = Ignition turned on` e `255 = Ignition turned off` em dois lugares independentes: são os DOIS LADOS de um par, não um evento genérico de "mudou", e o nome antigo apagava justamente a informação que o operador quer. E `50`, cadastrado na v4.16.0 como `Alerta de Reboque`, é `Device was plugged out` — a wiki da linha VL o rotula com a palavra solta "Puxar", que foi lida como guincho quando significa **o equipamento sendo arrancado da instalação** (irmão do `19`). Os dois erros são a mesma coisa: **havia fonte melhor e escolheu-se a mais curta**. Antes de batizar código novo, cruze com `https://docs.jimicloud.com/integration/integration.html` — as tabelas vêm no HTML servido (ver `jimi-doc-alarmes-no-html`), e ~100 códigos JIMI da doc ainda não estão no catálogo.
  - ⚠️ **Ao cadastrar um código, procure o MESMO evento no outro protocolo antes de escolher o nome.** Capotamento já existia como JIMI `45` sob o rótulo `Veículo Tumbado`; cadastrar `1047` como `Capotamento` e parar aí teria partido o evento em dois rótulos, e **o filtro da tela casa por nome** — o usuário escolheria um e perderia metade dos eventos. Mesma razão pela qual a v4.9.0 repetiu nomes de propósito nos pares 1024/1042.
  - ⚠️ **Alarme catalogado sem parâmetro em `occurrence_config_params` não gera ocorrência nenhuma** — `process_alarm_occurrence()` retorna cedo quando `get_occurrence_param()` devolve NULL. E a **categoria** decide se notifica: `Colisão do Veículo` está em `veiculo`, para a qual não há regra, e por isso **não dispara notificação** (registrado na v4.9.10, não corrigido — muda volume).
  - 🔴 **Anexo só entrava em `media_files` se o alarme gerasse OCORRÊNCIA — e vídeo extraído a pedido NÃO gera.** O único caminho que inseria era `link_media_to_occurrence()`, dentro do motor de ocorrências; evento de **diagnóstico** não passa por lá, e o `105 — Upload de Vídeo Concluído`, que é justamente quem anuncia o vídeo pedido por `HVIDEO`/`EVIDEO`, é diagnóstico. Resultado: o arquivo chegava íntegro ao disco e não existia para o sistema — invisível no playback, na fila de downloads e na galeria. Medido em produção (20/08/2026): **7 dos 12** eventos `105` das últimas 48 h. Corrigido na v4.9.35 com `media_register_file()` (`includes/media.php`), ponto **único** de registro, chamado pelo `pushalarm.php` para todo alarme com anexo; `link_media_to_occurrence()` delega para ela. ⚠️ Grave `file_url` **como o device o anunciou**: a JIMI manda os dois arquivos (frontal e interna) num campo só, separados por vírgula, e o motor de ocorrências casa pela string INTEIRA — partir cria linha que ele não reconhece.
  - 🔴 **A MESMA classe de bug, na tabela ERRADA: `link_upload_by_alarm_label()` (`includes/occurrence_engine.php`) só gravava `alarms.file_url` se o alarme já tivesse OCORRÊNCIA.** O `SELECT` fazia `JOIN occurrence_events`/`occurrences` ANTES de decidir gravar — alarme sem `occurrence_config_params` pro tipo (medido 26/08/2026: `264-3`, "ADAS: Distância Insegura") tinha o arquivo certo em `media_files` (o bullet acima já garantia isso) e `alarms.file_url` NULL **para sempre**, mesmo com upload 100% bem-sucedido. Sintoma: `rel_alarmes.php` e a aba "Alarmes Agrupados" mostravam `—` pro mesmo arquivo que a galeria via. Corrigido resolvendo o ALARME por `imei`+`alarm_label` sozinho, sem JOIN — o vínculo com `occurrences.media_file_id` continua existindo, mas como segundo passo opcional que não bloqueia mais o primeiro. **Qualquer função nova que grave em duas tabelas a partir de um evento (aqui: `alarms` sempre, `occurrences` só às vezes) tem de resolver e gravar a tabela INCONDICIONAL primeiro** — um `JOIN` até a tabela condicional na mesma query esconde a gravação da tabela incondicional atrás de uma condição que ela não deveria ter.

- 🔴 **Alertas Dirigibilidade (v4.21.0): `alarm_types.is_driving` separa CONDUÇÃO de videomonitoramento, por CÓDIGO — e código de condução sem a marca cai na tela de vídeo.** Decisão do dono do produto (14/09/2026): condução (arrancada, freada, curva, velocidade — inclusive 202 e 95 —, colisão, capotamento, impacto/inclinação) vai para `/relatorios/dirigibilidade` venha de câmera ou de rastreador, e **nenhuma** linha de condução tem função de vídeo (grade, detalhe de ocorrência, `/solicitarvideo`, pedido automático e backfill). `Alertas Videomonitoramento` (`/relatorios/alarmes`) exclui condução **e** equipamento sem câmera — alarme de rastreador que não é de condução fica só na ficha do veículo. **Migração que cadastra código de condução tem de marcar `is_driving` junto** (mesma armadilha do `risk_group`); ADAS de IA (FCW/PCW) não é condução. Leitura só pelos helpers de `includes/functions.php` (`alarm_driving_expr()`, `occurrence_no_video_sql()`, `is_driving_alarm()`, `device_has_camera_sql()`), que caem no comportamento anterior enquanto a coluna não existe. `tests/helpers/driving_alarms.test.php` trava a lista.
